{
  "name": "@advent-of-code/python",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "solve": "echo \"Error: no solve specified\" && exit 1",
    "test": "pytest",
    "create": "echo \"Error: no create specified\" && exit 1",
    "lint": "pylint src && mypy src",
    "format": "black .",
    "postinstall": "echo \"COMMENTED OUT pip install --upgrade -r requirements.txt\""
  }
}
